# --- CORE & NETWORKING ---
# asyncio is a Python built-in — do NOT pip install it
aiohttp          # Async HTTP client for Ollama/LLM communication
fastapi          # Backend for the Dashboard
uvicorn          # High-performance ASGI server to run the API

# --- DATABASE ---
aiosqlite        # Async SQLite driver (required for panel/history_store.py)

# --- OS & WINDOW DETECTION ---
psutil           # System process and memory monitoring
# Windows specific (Conditional)
pywin32; sys_platform == 'win32'
# Linux specific (Conditional)
ewmh; sys_platform == 'linux'
# macOS specific (Conditional)
pyobjc-framework-AppKit; sys_platform == 'darwin'
pyobjc-framework-Quartz; sys_platform == 'darwin'

# --- TOOLS & AUTOMATION (The "Hands") ---
pyautogui        # Cross-platform UI automation (Click/Type)
mouse            # Fine-grained mouse control fallback
keyboard         # Global hotkey listening and simulation
pillow           # Image processing for screenshots and vision fallback
faster-whisper   # STT (was 'fast-whisper' — correct package name is faster-whisper)
pyaudio
silero-vad       # VAD (correct PyPI name is silero-vad, not silero_vad)
pyttsx3
openwakeword
noisereduce      # Spectral noise gating (was misspelled 'noicereduce')

# --- NEURAL NOISE SUPPRESSION (Optional, CPU-only) ---
# Uncomment to enable DeepFilterNet neural noise reduction:
# deepfilternet
# torch            # Required by deepfilternet; install CPU-only build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- BRAIN, VECTOR DB & MATH ---
numpy            # Fast matrix math for cosine similarity calculations
scikit-learn     # Vector distance calculations
chromadb         # Vector store for memory/capability mapping

# --- UTILITIES ---
python-dotenv    # For managing API keys and local configs in core/config.py
pydantic>=2.0.0  # Data validation (FastAPI dependency)

# --- TESTING ---
pytest
pytest-asyncio