# --- CORE & NETWORKING ---
asyncio          # Asynchronous I/O for the Event Bus and Orchestrator
aiohttp          # Async HTTP client for Ollama/LLM communication
fastapi          # Backend for the Dashboard
uvicorn          # High-performance ASGI server to run the API
pydantic          # Data validation for the API and Settings

# --- OS & WINDOW DETECTION ---
psutil           # System process and memory monitoring
# Windows specific (Conditional)
pywin32; sys_platform == 'win32'
# Linux specific (Conditional)
ewmh; sys_platform == 'linux'
# macOS specific (Conditional)
pyobjc-framework-AppKit; sys_platform == 'darwin'
pyobjc-framework-Quartz; sys_platform == 'darwin'

# --- TOOLS & AUTOMATION (The "Hands") ---
pyautogui        # Cross-platform UI automation (Click/Type)
mouse            # Fine-grained mouse control fallback
keyboard         # Global hotkey listening and simulation
pillow           # Image processing for screenshots and vision fallback
fast-whisper
pyaudio
silero_vad
pyttsx3
openwakeword

# --- 🔄 NEW: BRAIN, VECTOR DB & MATH ---
# These are strictly required for CapabilityMapper's numpy math and vector store!
numpy            # Fast matrix math for cosine similarity calculations
scikit-learn     # Optional but great for heavy vector distance checks
chromadb

# --- UTILITIES ---
python-dotenv    # For managing API keys and local configs in core/config.py
# (Removed 'logging' as it is a native Python built-in and cannot be pip installed!)