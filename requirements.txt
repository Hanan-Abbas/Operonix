# --- CORE & NETWORKING ---
# asyncio is a Python built-in — do NOT pip install it
aiohttp          # Async HTTP client for Ollama/LLM communication
fastapi          # Backend for the Dashboard
uvicorn          # High-performance ASGI server to run the API

# --- DATABASE ---
aiosqlite        # Async SQLite driver (required for panel/history_store.py)

# --- OS & WINDOW DETECTION ---
psutil           # System process and memory monitoring
# Windows specific (Conditional)
pywin32; sys_platform == 'win32'
# Linux specific (Conditional)
ewmh; sys_platform == 'linux'
# macOS specific (Conditional)
pyobjc-framework-AppKit; sys_platform == 'darwin'
pyobjc-framework-Quartz; sys_platform == 'darwin'

# --- TOOLS & AUTOMATION (The "Hands") ---
pyautogui
mouse
keyboard
pillow
faster-whisper
pyaudio
silero-vad
pyttsx3
openwakeword
noisereduce

# --- NEURAL NOISE SUPPRESSION (CRITICAL: Python 3.12 Sync) ---
# We use 2.2.0 specifically to maintain 'torchaudio.backend' for DeepFilterNet 0.5.6
torch==2.2.0 --index-url https://download.pytorch.org/whl/cpu
torchaudio==2.2.0 --index-url https://download.pytorch.org/whl/cpu
deepfilternet==0.5.6
deepfilterlib==0.5.6

# --- BRAIN, VECTOR DB & MATH ---
# Pin packaging to keep DeepFilterNet happy and prevent ChromaDB from upgrading it
packaging>=23.0,<24.0
numpy<2.0.0      # DeepFilterNet requires numpy < 2.0
scikit-learn
chromadb

# --- UTILITIES ---
python-dotenv    # For managing API keys and local configs in core/config.py
pydantic>=2.0.0  # Data validation (FastAPI dependency)

# --- TESTING ---
pytest
pytest-asyncio